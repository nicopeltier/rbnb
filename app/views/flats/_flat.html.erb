
    <div class="card">
        <div>
          <% if f.picture.url %>
            <%= image_tag f.picture.url(:big) %>
          <% else %>
            no picture
          <% end %>
        </div>
        <div><h3>$:<%= f.price %> | <%= link_to f.short_description, flat_path(f) %></h3>
        </div>
    </div>
